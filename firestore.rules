rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow access to everything for authenticated users temporarily to debug
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}